RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.infysmart\.com$ [NC]
RewriteRule ^(.*)$ https://infysmart.com/$1 [L,R=301]

# Enforce HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
