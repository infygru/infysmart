// Placeholder — client logos are handled by ClientStrip.tsx which reads from Directus CMS.
// This component exists for legacy import compatibility.
export default function OurClients() {
  return null;
}
